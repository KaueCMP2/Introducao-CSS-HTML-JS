let numero = parseInt (prompt("Digite um numero"))
numero = parseInt(numero)

if (numero > 0)
    alert ("Numero positivo")

else if (numero < 0)
    alert ("Numero negativo")

else
    alert ("Erro seu numero é: " + numero)